#include<stdio.h>
int main()
{
    int a[50];
    int a1[51];
    int sum
    for(int i=0;i<50;i++)
    {
        scanf("%d",&a[i]);
    }
    for(int i=0;i<50;i++)
    {
        scanf("%d",&a1[i]);
    }

    for(int i=50;i>=0;i--)
    {
        sum=a[i]+a1[i];
        if(sum>10)
        {
            num=sum/10;
            a1[i]=sum%10
        }
         count++;
         if(count==1)
         {
            dup=num;
         }
         if(count==2)
         {
            
         }
    }
}
    